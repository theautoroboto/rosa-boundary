{
  "scan_date": "2026-05-18",
  "commit": "6e079f7",
  "findings": [
    {
      "id": "L1",
      "title": "Reflected XSS in OAuth Callback Error Page",
      "severity": "low",
      "category": "Application \u2014 Cross-Site Scripting (XSS)",
      "file": "internal/auth/callback.go",
      "line": 60,
      "end_line": null,
      "issue": "The OAuth callback handler writes error messages from query parameters into HTML after HTML-escaping and sanitizing control characters. The fix (html.EscapeString + sanitizeOAuthParam) is present. This finding is resolved.",
      "impact": "Previously exploitable for phishing via localhost callback. Now mitigated.",
      "recommendation": "No action required \u2014 fix is in place at line 60 using html.EscapeString."
    },
    {
      "id": "L2",
      "title": "EFS Security Group Allows All Egress",
      "severity": "low",
      "category": "Infrastructure \u2014 Overly Permissive Network Configuration",
      "file": "deploy/regional/efs.tf",
      "line": 31,
      "end_line": 36,
      "issue": "The EFS security group allows all outbound traffic to `0.0.0.0/0` on all ports and protocols. EFS mount targets only need inbound NFS (port 2049) and do not initiate outbound connections.",
      "impact": "Minimal direct risk, but defense-in-depth suggests removing unnecessary egress from the EFS security group.",
      "recommendation": "Remove the egress rule from the EFS security group entirely, or scope it to only the VPC CIDR if required:\n\n```hcl\nresource \"aws_security_group\" \"efs\" {\n  # Remove the egress block entirely for EFS mount targets\n  ...\n}\n```"
    },
    {
      "id": "L3",
      "title": "Example Script Uses assignPublicIp=ENABLED",
      "severity": "low",
      "category": "Infrastructure \u2014 Network Exposure",
      "file": "deploy/regional/examples/launch_task.sh",
      "line": 62,
      "end_line": null,
      "issue": "The example launch script uses `assignPublicIp=ENABLED` when launching Fargate tasks. The production Lambda handler correctly uses `DISABLED`.",
      "impact": "If an operator copies this example script for production use, Fargate tasks would receive public IP addresses, exposing the SSM agent and container services to the internet. The example is in `examples/` and is not used by automation, but it sets a risky precedent.",
      "recommendation": "Change to `assignPublicIp=DISABLED` in the example script, or add a warning comment explaining the security implications."
    },
    {
      "id": "L4",
      "title": "oc_version Not Validated in Lambda Handler",
      "severity": "low",
      "category": "Application \u2014 Input Validation",
      "file": "lambda/create-investigation/handler.py",
      "line": 132,
      "end_line": null,
      "issue": "The `oc_version` parameter from the request body is accepted without validation and passed directly into the task definition environment variables and task tags. While `validate_identifier()` is called for `investigation_id` and `cluster_id`, `oc_version` is not validated against the set of valid versions (4.14-4.20).",
      "impact": "A malicious or errant client could inject arbitrary strings into the `OC_VERSION` environment variable and task tags. The entrypoint script does validate the value (`if [ -x \"/opt/openshift/${OC_VERSION}/oc\" ]`) so invalid versions would fall back to default, but the unvalidated tag value could pollute tagging/auditing.",
      "recommendation": "Add validation for `oc_version` in the Lambda handler:\n\n```python\nVALID_OC_VERSIONS = {'4.14', '4.15', '4.16', '4.17', '4.18', '4.19', '4.20'}\nif oc_version not in VALID_OC_VERSIONS:\n    return response(400, {'error': f'Invalid oc_version: must be one of {sorted(VALID_OC_VERSIONS)}'})\n```"
    },
    {
      "id": "L5",
      "title": "Terraform AWS Provider Broadly Pinned and Lock File Gitignored",
      "severity": "low",
      "category": "Infrastructure \u2014 Unpinned Dependency Version",
      "file": "deploy/regional/main.tf",
      "line": 7,
      "end_line": null,
      "issue": "The AWS provider is pinned with `version = \"~> 5.0\"`, which allows any 5.x release. The `.terraform.lock.hcl` file is explicitly gitignored in both the root `.gitignore` and `deploy/regional/.gitignore`, meaning different Terraform runs on different machines could use different provider versions without detection.",
      "impact": "A compromised or buggy provider release within the 5.x range could be automatically used in CI or by operators. Without a committed lock file, there is no mechanism to detect provider version drift between runs.",
      "recommendation": "Pin to a more specific range (e.g., `\"~> 5.80\"`) and remove `.terraform.lock.hcl` from `.gitignore`, then commit it:\n\n```\n# In .gitignore: remove the .terraform.lock.hcl line\ngit add deploy/regional/.terraform.lock.hcl\ngit commit -m 'chore: commit Terraform provider lock file'\n```"
    },
    {
      "id": "L6",
      "title": "Unpinned Fedora Base Image",
      "severity": "low",
      "category": "Infrastructure \u2014 Unpinned Image Tag",
      "file": "Containerfile",
      "line": 3,
      "end_line": null,
      "issue": "The base image `FROM fedora:43` is pinned to the major version but not to a specific digest. While `fedora:43` is better than `fedora:latest`, the tag can be overwritten when new builds of Fedora 43 are published.",
      "impact": "Rebuilds of the container could silently pull a different base image with different packages or configurations. For a security-sensitive SRE tooling container, reproducible builds are important.",
      "recommendation": "Pin to a digest:\n\n```dockerfile\nFROM fedora:43@sha256:<digest>\n```"
    },
    {
      "id": "L7",
      "title": "Claude Code Installed via Piped Shell Script Without Integrity Check",
      "severity": "low",
      "category": "Application \u2014 Supply Chain",
      "file": "Containerfile",
      "line": 72,
      "end_line": null,
      "issue": "Claude Code is installed by piping a remote script to bash: `curl -fsSL https://claude.ai/install.sh | INSTALL_DIR=/usr/local/lib/claude-code BIN_DIR=/usr/local/bin bash`. This pattern is vulnerable to man-in-the-middle or compromised CDN attacks at build time. There is no checksum or signature verification.",
      "impact": "A compromised install script could inject malicious code into the container image. The `-fsSL` flags ensure the download is from HTTPS, reducing MITM risk, but there is no integrity verification (checksum, signature).",
      "recommendation": "If Claude Code publishes checksums or release signatures, verify them after download. Alternatively, download the installer in a separate step and verify its hash before executing."
    },
    {
      "id": "L8",
      "title": "GitHub Actions Pinned to Mutable Version Tags, Not SHAs",
      "severity": "low",
      "category": "Application \u2014 Supply Chain",
      "file": ".github/workflows/localstack-tests.yml",
      "line": 32,
      "end_line": null,
      "issue": "All GitHub Actions in both CI workflows are pinned to mutable version tags (e.g., `actions/checkout@v4`, `codecov/codecov-action@v4`, `EnricoMi/publish-unit-test-result-action/composite@v2`) rather than immutable commit SHAs. A malicious actor who compromises these action repositories could push a new tag pointing to malicious code.",
      "impact": "For a FedRAMP system, supply chain integrity of CI/CD tooling is important. A compromised action with repository-level write permissions could exfiltrate secrets, modify deployment artifacts, or tamper with test results. The `localstack-tests` workflow has access to `LOCALSTACK_AUTH_TOKEN`.",
      "recommendation": "Pin all actions to their SHA:\n\n```yaml\nuses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2\nuses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2f  # v5.3.0\nuses: github/codeql-action/upload-sarif@v3  # pin to SHA\n```\n\nUse `renovate` or `dependabot` with `pin-digests: true` to automate SHA pinning."
    },
    {
      "id": "L8",
      "title": "GitHub Actions Pinned to Mutable Version Tags, Not SHAs",
      "severity": "low",
      "category": "Application \u2014 Supply Chain",
      "file": ".github/workflows/upload-sarif.yml",
      "line": 28,
      "end_line": null,
      "issue": "All GitHub Actions in both CI workflows are pinned to mutable version tags (e.g., `actions/checkout@v4`, `codecov/codecov-action@v4`, `EnricoMi/publish-unit-test-result-action/composite@v2`) rather than immutable commit SHAs.",
      "impact": "A compromised action with repository-level write permissions could exfiltrate secrets, modify deployment artifacts, or tamper with security findings before SARIF upload.",
      "recommendation": "Pin all actions to their SHA. Use renovate with `pin-digests: true` to automate SHA pinning."
    },
    {
      "id": "L9",
      "title": "uv Install Script in CI Piped Without Integrity Verification",
      "severity": "low",
      "category": "Application \u2014 Supply Chain",
      "file": ".github/workflows/localstack-tests.yml",
      "line": 41,
      "end_line": null,
      "issue": "Both `localstack-tests` and `lambda-unit-tests` jobs install `uv` by piping a remote script to `sh`: `curl -LsSf https://astral.sh/uv/install.sh | sh`. While HTTPS is used, there is no hash verification of the installer itself.",
      "impact": "A compromised astral.sh CDN could serve a malicious installer to CI, potentially affecting Lambda build reproducibility or test validity in a FedRAMP pipeline.",
      "recommendation": "Use the official `astral-sh/setup-uv` GitHub Action (pinned to a SHA) instead:\n\n```yaml\n- uses: astral-sh/setup-uv@...<SHA>...\n  with:\n    version: '0.4.x'\n```\n\nThis avoids the curl-pipe pattern and leverages GitHub's action verification."
    },
    {
      "id": "L10",
      "title": "Default log_retention_days of 7 Days Is Insufficient for FedRAMP",
      "severity": "low",
      "category": "Infrastructure \u2014 Insufficient Log Retention",
      "file": "deploy/regional/variables.tf",
      "line": 88,
      "end_line": null,
      "issue": "The `log_retention_days` variable defaults to 7 days. This applies to all Lambda CloudWatch log groups (`/aws/lambda/...`) and the ECS container log group (`/ecs/...`). FedRAMP (NIST SP 800-53 AU-11) requires audit records to be retained for at least 90 days online with 1 year offline, and the CIS AWS Foundations Benchmark requires CloudWatch log groups to have a retention period of at least 365 days.",
      "impact": "Operational logs for Lambda invocations (including OIDC validation errors, task creation events, and ECS exec sessions) would be automatically deleted after 7 days. An incident discovered after 7 days would have no supporting log evidence. This directly violates FedRAMP AU-11 audit record retention requirements.",
      "recommendation": "Increase the default to meet FedRAMP minimums:\n\n```hcl\nvariable \"log_retention_days\" {\n  default = 365  # FedRAMP requires 1 year minimum\n}\n```\n\nConsider separate `active_log_retention_days` (90 days, CloudWatch) and `archive_log_retention_days` (1 year, S3 export) to manage cost."
    },
    {
      "id": "M1",
      "title": "Lambda ECS IAM Policy Uses Wildcard Resource",
      "severity": "medium",
      "category": "Infrastructure \u2014 Overly Permissive IAM Policy",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 55,
      "end_line": null,
      "issue": "The Lambda IAM policy grants `ecs:RunTask`, `ecs:StopTask`, `ecs:ListTasks`, `ecs:DescribeTasks`, `ecs:DescribeTaskDefinition`, `ecs:RegisterTaskDefinition`, `ecs:DeregisterTaskDefinition`, and `ecs:TagResource` on `Resource = \"*\"`. This allows the Lambda function to manage tasks and task definitions across any ECS cluster in the account.",
      "impact": "If the Lambda is compromised (e.g., via a token validation bypass or dependency supply chain attack), the attacker can launch, stop, or register task definitions in any cluster in the AWS account.",
      "recommendation": "Scope ECS resources to the specific cluster:\n\n```hcl\nResource = [\n  aws_ecs_cluster.main.arn,\n  \"arn:aws:ecs:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:task/${aws_ecs_cluster.main.name}/*\",\n  \"arn:aws:ecs:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:task-definition/${var.project}-${var.stage}*\"\n]\n```\n\nNote: `RegisterTaskDefinition` and `DeregisterTaskDefinition` must use `*`, but `RunTask`, `StopTask`, and `TagResource` can be scoped."
    },
    {
      "id": "M2",
      "title": "Shared SRE Role Grants KMS Decrypt on All Keys",
      "severity": "medium",
      "category": "Infrastructure \u2014 Overly Permissive IAM Policy",
      "file": "deploy/regional/oidc.tf",
      "line": 222,
      "end_line": 230,
      "issue": "The `KMSForECSExec` statement in the shared SRE role's policy grants `kms:Decrypt` and `kms:GenerateDataKey` on `Resource = \"*\"`. This allows any SRE who assumes this role to decrypt data encrypted with any KMS key in the account (subject to key policy).",
      "impact": "An SRE with this role could potentially decrypt data outside the scope of ECS Exec sessions if other KMS keys have permissive key policies. The task role (`aws_iam_role_policy.task_kms` at `iam.tf:224`) correctly scopes to `aws_kms_key.exec_session.arn`, making this inconsistency notable.",
      "recommendation": "Scope the KMS resource to the specific exec session key:\n\n```hcl\nResource = aws_kms_key.exec_session.arn\n```"
    },
    {
      "id": "M3",
      "title": "CloudWatch Logs Encryption Disabled for SSM Sessions",
      "severity": "medium",
      "category": "Infrastructure \u2014 Encryption Gap",
      "file": "deploy/regional/ecs.tf",
      "line": 17,
      "end_line": null,
      "issue": "The ECS cluster's execute command configuration sets `cloud_watch_encryption_enabled = false`. The CloudWatch log group for SSM sessions (`ssm_sessions`) also lacks a `kms_key_id` attribute. All Lambda CloudWatch log groups and the ECS container log group also lack CMK encryption.",
      "impact": "SSM session logs may contain sensitive commands and output from SRE investigations into production clusters. Without CMK encryption on log groups, there is no KMS key policy gate on who can read these audit logs. AWS account-level admins and anyone with CloudWatch Logs `GetLogEvents` can read the logs without a separate KMS authorization check.",
      "recommendation": "Enable CloudWatch encryption for ECS Exec and attach the KMS key to all sensitive log groups:\n\n```hcl\ncloud_watch_encryption_enabled = true\n```\n\nAdd `kms_key_id = aws_kms_key.exec_session.arn` to the `ssm_sessions`, `rosa_boundary`, and Lambda log groups."
    },
    {
      "id": "M4",
      "title": "S3 Audit Bucket Uses SSE-S3 Instead of SSE-KMS",
      "severity": "medium",
      "category": "Infrastructure \u2014 Encryption Configuration",
      "file": "deploy/regional/s3.tf",
      "line": 48,
      "end_line": null,
      "issue": "The S3 audit bucket uses `sse_algorithm = \"AES256\"` (SSE-S3) instead of `aws:kms` (SSE-KMS). SSE-S3 uses AWS-managed keys with no audit trail for key usage and no ability to control access via KMS key policies.",
      "impact": "For an audit log bucket with WORM compliance (Object Lock), the inability to independently audit encryption key usage or enforce key-level access controls weakens the security posture. Any principal with `s3:GetObject` permission can decrypt objects; there is no separate `kms:Decrypt` gate. This violates the FedRAMP SC-28 (Protection of Information at Rest) enhanced requirement for separate key management.",
      "recommendation": "Use KMS encryption with a dedicated CMK:\n\n```hcl\napply_server_side_encryption_by_default {\n  sse_algorithm     = \"aws:kms\"\n  kms_master_key_id = aws_kms_key.audit_bucket.arn\n}\n```"
    },
    {
      "id": "M5",
      "title": "No EFS Filesystem Policy Enforcing IAM Authorization",
      "severity": "medium",
      "category": "Infrastructure \u2014 Missing Access Control",
      "file": "deploy/regional/efs.tf",
      "line": 1,
      "end_line": null,
      "issue": "The EFS filesystem has no `aws_efs_file_system_policy` resource. While individual access points require IAM (`iam = \"ENABLED\"` in the task definition), the filesystem itself does not enforce IAM authorization or deny anonymous access at the filesystem level. Any entity with network access to the mount targets (port 2049) could potentially mount the filesystem directly without an access point.",
      "impact": "If the Fargate security group or VPC network controls are misconfigured, an entity with network access to NFS port 2049 could mount the EFS filesystem as root, bypassing access point isolation and accessing all investigation data across all users.",
      "recommendation": "Add a filesystem policy that enforces IAM authentication and access-point-only access:\n\n```hcl\nresource \"aws_efs_file_system_policy\" \"sre_home\" {\n  file_system_id = aws_efs_file_system.sre_home.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [\n      {\n        Effect    = \"Allow\"\n        Principal = { AWS = \"*\" }\n        Action    = [\"elasticfilesystem:ClientMount\", \"elasticfilesystem:ClientWrite\"]\n        Resource  = aws_efs_file_system.sre_home.arn\n        Condition = {\n          Bool = { \"elasticfilesystem:AccessedViaMountTarget\" = \"true\" }\n          StringEquals = {\n            \"elasticfilesystem:AccessPointArn\" = \"arn:aws:elasticfilesystem:*:*:access-point/*\"\n          }\n        }\n      },\n      {\n        Effect    = \"Deny\"\n        Principal = { AWS = \"*\" }\n        Action    = \"elasticfilesystem:*\"\n        Resource  = aws_efs_file_system.sre_home.arn\n        Condition = { Bool = { \"elasticfilesystem:AccessedViaMountTarget\" = \"false\" } }\n      }\n    ]\n  })\n}\n```"
    },
    {
      "id": "M6",
      "title": "No S3 Bucket Policy Enforcing Encryption in Transit",
      "severity": "medium",
      "category": "Infrastructure \u2014 Missing Access Control and Auditability",
      "file": "deploy/regional/s3.tf",
      "line": 1,
      "end_line": null,
      "issue": "The S3 audit bucket has no `aws_s3_bucket_policy` to enforce encryption in transit (deny non-TLS requests). Without a deny-non-TLS bucket policy, objects could theoretically be uploaded or accessed over unencrypted HTTP.",
      "impact": "Without a deny-non-TLS policy, S3 SDK misconfigurations or internal tooling could access audit data over plaintext HTTP. FedRAMP SC-8 (Transmission Confidentiality and Integrity) requires encryption in transit, and the CIS AWS Foundations Benchmark explicitly requires an S3 bucket policy denying non-secure transport.",
      "recommendation": "Add a bucket policy requiring TLS:\n\n```hcl\nresource \"aws_s3_bucket_policy\" \"audit\" {\n  bucket = aws_s3_bucket.audit.id\n  policy = jsonencode({\n    Version = \"2012-10-17\"\n    Statement = [{\n      Sid       = \"DenyNonTLS\"\n      Effect    = \"Deny\"\n      Principal = \"*\"\n      Action    = \"s3:*\"\n      Resource  = [\n        \"${aws_s3_bucket.audit.arn}\",\n        \"${aws_s3_bucket.audit.arn}/*\"\n      ]\n      Condition = {\n        Bool = { \"aws:SecureTransport\" = \"false\" }\n      }\n    }]\n  })\n}\n```"
    },
    {
      "id": "M7",
      "title": "Lambda CORS Allows All Origins",
      "severity": "medium",
      "category": "Application \u2014 Overly Permissive CORS",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 167,
      "end_line": null,
      "issue": "Both the Lambda function URL CORS configuration (`allow_origins = [\"*\"]`) and the Lambda handler's response headers (`Access-Control-Allow-Origin: *`) permit cross-origin requests from any domain. The Terraform comment says \"Allow localhost for testing; restrict in production\", but this is not stage-gated.",
      "impact": "A malicious website could make cross-origin requests to the Lambda function URL. Since the primary auth mechanism is SigV4 (which cannot be replicated cross-origin) and the OIDC token is passed as a custom header, actual exploitation risk is low. However, the wildcard CORS combined with `Access-Control-Allow-Headers: content-type, x-oidc-token` means if an attacker obtains a valid OIDC token, they could invoke the function from any origin.",
      "recommendation": "Restrict CORS origins to known SRE tool domains or remove CORS entirely if the function is only called via direct SDK invocation (which is the current architecture per the lambda-invoker role)."
    },
    {
      "id": "M7",
      "title": "Lambda CORS Allows All Origins",
      "severity": "medium",
      "category": "Application \u2014 Overly Permissive CORS",
      "file": "lambda/create-investigation/handler.py",
      "line": 804,
      "end_line": null,
      "issue": "The Lambda handler's `response()` function returns `Access-Control-Allow-Origin: *` on every response, permitting cross-origin requests from any domain.",
      "impact": "See M7 above \u2014 both the Terraform CORS configuration and the handler-level CORS header are wildcard. Both must be fixed.",
      "recommendation": "Change to `Access-Control-Allow-Origin: https://your-sre-portal.example.com` or remove the header entirely if the Lambda is only invoked via direct AWS SDK calls."
    },
    {
      "id": "M8",
      "title": "Container Grants Passwordless Sudo ALL to sre User",
      "severity": "medium",
      "category": "Application \u2014 Container Privilege Escalation",
      "file": "Containerfile",
      "line": 66,
      "end_line": null,
      "issue": "The SRE user is granted `NOPASSWD: ALL` sudo access: `echo 'sre ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/sre`. This allows any user who connects via ECS Exec to escalate to root inside the container.",
      "impact": "A compromised SRE session or an insider threat can escalate to root inside the container. Root access enables modification of entrypoint/cleanup scripts (bypassing S3 audit sync), reading other users' EFS data if access point isolation is bypassed, installing tools, or attempting lateral movement via the task role's IAM credentials. All container processes share the same task role credentials regardless of the Linux user.",
      "recommendation": "If root access is required for specific operations (e.g., `alternatives --set`), restrict sudo to specific commands:\n\n```\nsre ALL=(ALL) NOPASSWD: /usr/sbin/alternatives\n```\n\nThe entrypoint already runs as root and handles `alternatives` switching before the user connects, so the SRE user may not need sudo at all."
    },
    {
      "id": "M9",
      "title": "Python Lambda Dependencies Use >= Version Pinning in pyproject.toml",
      "severity": "medium",
      "category": "Application \u2014 Unpinned Dependency Version",
      "file": "lambda/create-investigation/pyproject.toml",
      "line": 6,
      "end_line": 9,
      "issue": "Lambda dependencies use `>=` version specifiers in `pyproject.toml`: `PyJWT>=2.8.0`, `cryptography>=41.0.0`, `requests>=2.31.0`, `boto3>=1.34.0`. Although a `uv.lock` file is committed (which pins exact versions for local development), the Lambda Makefile's `deps` target runs `uv export --frozen` which resolves from the lock file. However, the loose pyproject.toml specifiers mean that updating the lockfile (`uv lock --upgrade`) could pull in any newer version, and there is no lock file hash verification in the deployment pipeline.",
      "impact": "Supply chain risk: a compromised version of PyJWT, cryptography, or requests could be automatically pulled in when the lock file is updated. The `cryptography` package in particular has been targeted in supply chain attacks. For FedRAMP SA-12 (Supply Chain Protection), the deployment pipeline should use exact, hash-verified dependencies.",
      "recommendation": "Pin exact versions with hashes in pyproject.toml:\n\n```toml\ndependencies = [\n    \"PyJWT==2.8.0\",\n    \"cryptography==42.0.8\",\n    \"requests==2.32.3\",\n    \"boto3==1.34.150\",\n]\n```\n\nVerify uv.lock hash integrity in CI before building the Lambda package."
    },
    {
      "id": "M10",
      "title": "Bedrock IAM Policy Allows All Regions and All Models",
      "severity": "medium",
      "category": "Infrastructure \u2014 Overly Permissive IAM Policy",
      "file": "deploy/regional/iam.tf",
      "line": 155,
      "end_line": 165,
      "issue": "The Bedrock policy grants `bedrock:InvokeModel` and `bedrock:InvokeModelWithResponseStream` on `arn:aws:bedrock:*:*:inference-profile/*` and `arn:aws:bedrock:*:*:foundation-model/*`. The wildcard regions and all-model access mean the task role can invoke any foundation model in any region.",
      "impact": "If an SRE's container is compromised, the attacker can invoke arbitrary Bedrock models (potentially expensive ones) in any region, leading to cost abuse. Additionally, data sent to Bedrock models outside the expected region may violate data residency requirements \u2014 a critical concern for FedRAMP where data must remain in authorized US regions.",
      "recommendation": "Scope to the specific region and model(s) needed:\n\n```hcl\nResource = [\n  \"arn:aws:bedrock:${data.aws_region.current.name}::foundation-model/anthropic.claude-*\",\n  \"arn:aws:bedrock:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:inference-profile/*\"\n]\n```"
    },
    {
      "id": "M11",
      "title": "ECS Task and Lambda IAM Roles Lack Confused Deputy Protection",
      "severity": "medium",
      "category": "Infrastructure \u2014 IAM Trust Policy Weakness",
      "file": "deploy/regional/iam.tf",
      "line": 106,
      "end_line": 121,
      "issue": "The ECS task role, ECS execution role, and Lambda execution roles all use bare service principal trust policies without `aws:SourceAccount` or `aws:SourceArn` conditions:\n\n```json\n{\"Principal\": {\"Service\": \"ecs-tasks.amazonaws.com\"}, \"Action\": \"sts:AssumeRole\"}\n```\n\nThis pattern is vulnerable to the confused deputy problem: any ECS task in the same account (or cross-account if SCP allows) could potentially assume these roles if the service principal is not conditioned.",
      "impact": "While ECS role assumption is constrained by task definition ARN in practice, lacking `aws:SourceArn` or `aws:SourceAccount` conditions means the roles are technically assumable by any ECS task in the account that can register an appropriate task definition. For Lambda roles, this means any Lambda in the account (with IAM permission to assume) could assume the execution role. The missing condition weakens the blast radius isolation between different ECS workloads sharing an account.",
      "recommendation": "Add source conditions to all ECS and Lambda trust policies:\n\n```hcl\nCondition = {\n  StringEquals = {\n    \"aws:SourceAccount\" = data.aws_caller_identity.current.account_id\n  }\n  ArnLike = {\n    \"aws:SourceArn\" = \"arn:aws:ecs:${data.aws_region.current.name}:${data.aws_caller_identity.current.account_id}:*\"\n  }\n}\n```"
    },
    {
      "id": "M11",
      "title": "ECS Task and Lambda IAM Roles Lack Confused Deputy Protection",
      "severity": "medium",
      "category": "Infrastructure \u2014 IAM Trust Policy Weakness",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 12,
      "end_line": 27,
      "issue": "The create-investigation Lambda execution role trust policy lacks `aws:SourceAccount` or `aws:SourceArn` conditions on the `lambda.amazonaws.com` principal.",
      "impact": "See M11 above \u2014 any Lambda function in the account could attempt to assume this role if granted `sts:AssumeRole` by some other policy.",
      "recommendation": "Add `aws:SourceAccount` condition to Lambda trust policies."
    },
    {
      "id": "M12",
      "title": "Lambda Missing elasticfilesystem:DeleteAccessPoint Permission for Error Cleanup",
      "severity": "medium",
      "category": "Application \u2014 Missing IAM Permission Causing Silent Failures",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 70,
      "end_line": 88,
      "issue": "The Lambda's EFS IAM policy (`efs-access-point-management`) grants only `CreateAccessPoint`, `DescribeAccessPoints`, and `TagResource`. However, the Lambda handler calls `efs.delete_access_point()` in four error-cleanup paths (lines 671, 727, 756, 782 in handler.py). These calls will silently fail with `AccessDeniedException` because the permission is not granted.",
      "impact": "When task registration or task launch fails after an EFS access point was newly created, the cleanup code cannot delete the orphaned access point. This leads to EFS access point accumulation. The EFS access point limit is 10,000 per filesystem. In a sustained attack or failure storm, the limit could be reached, preventing new investigations from being created. Additionally, orphaned access points (with their embedded `cluster_id`/`investigation_id` tags and OIDC `sub` values) linger without audit trail.",
      "recommendation": "Add `elasticfilesystem:DeleteAccessPoint` to the Lambda EFS policy, scoped to the filesystem ARN:\n\n```hcl\n{\n  Effect = \"Allow\"\n  Action = [\n    \"elasticfilesystem:CreateAccessPoint\",\n    \"elasticfilesystem:DescribeAccessPoints\",\n    \"elasticfilesystem:DeleteAccessPoint\",\n    \"elasticfilesystem:TagResource\"\n  ]\n  Resource = aws_efs_file_system.sre_home.arn\n}\n```"
    },
    {
      "id": "M12",
      "title": "Lambda Missing elasticfilesystem:DeleteAccessPoint Permission for Error Cleanup",
      "severity": "medium",
      "category": "Application \u2014 Missing IAM Permission Causing Silent Failures",
      "file": "lambda/create-investigation/handler.py",
      "line": 671,
      "end_line": 782,
      "issue": "The handler calls `efs.delete_access_point()` in four error-cleanup paths but the Lambda execution role lacks `elasticfilesystem:DeleteAccessPoint` permission. The calls are silently swallowed by bare `except Exception: pass` blocks.",
      "impact": "Orphaned EFS access points accumulate silently on every failed task launch. See M12 above for full impact.",
      "recommendation": "Grant `elasticfilesystem:DeleteAccessPoint` in the Lambda IAM policy. Also replace bare `except Exception: pass` with logging so failures are visible in CloudWatch."
    },
    {
      "id": "M13",
      "title": "ECS Task Role Grants s3:PutObjectAcl on Audit Bucket",
      "severity": "medium",
      "category": "Infrastructure \u2014 Overly Permissive IAM Policy",
      "file": "deploy/regional/iam.tf",
      "line": 131,
      "end_line": 137,
      "issue": "The ECS task role's S3 policy grants `s3:PutObjectAcl` on the audit bucket. The audit bucket has `block_public_acls = true` and `ignore_public_acls = true` which prevents making objects public, but `PutObjectAcl` is still an unnecessary permission that violates least privilege. With Object Lock in COMPLIANCE mode, ACLs cannot be used to bypass retention, but the permission is still wider than needed.",
      "impact": "An SRE container with the task role could set object ACLs on audit log objects (e.g., grant read access to another canonical user). While public ACLs are blocked, cross-account ACL grants are not prevented by the public access block settings. This could allow an insider to make audit evidence readable by an attacker-controlled AWS account.",
      "recommendation": "Remove `s3:PutObjectAcl` from the task role policy. The audit sync only needs `s3:PutObject`, `s3:GetObject`, and `s3:ListBucket`."
    },
    {
      "id": "M14",
      "title": "EFS Not Encrypted with Customer-Managed KMS Key",
      "severity": "medium",
      "category": "Infrastructure \u2014 Encryption Configuration",
      "file": "deploy/regional/efs.tf",
      "line": 2,
      "end_line": 4,
      "issue": "The EFS filesystem uses `encrypted = true` but does not specify a `kms_key_id`. Without an explicit key, AWS uses the aws/elasticfilesystem AWS-managed key, which does not allow custom key policies, cross-account access control, or independent key usage audit via CloudTrail's KMS data events.",
      "impact": "For FedRAMP SC-28 (Protection of Information at Rest), using an AWS-managed key rather than a CMK means there is no way to restrict which IAM principals can access the encryption key, no ability to rotate the key on demand, and no separate CloudTrail audit of key usage. Investigation data (SRE home directories) could be decrypted by any principal with `elasticfilesystem:*` access without a separate KMS gate.",
      "recommendation": "Create a dedicated KMS CMK for EFS encryption:\n\n```hcl\nresource \"aws_efs_file_system\" \"sre_home\" {\n  encrypted  = true\n  kms_key_id = aws_kms_key.efs_sre_home.arn\n  ...\n}\n```"
    },
    {
      "id": "H1",
      "title": "No CloudTrail or GuardDuty Configuration in Terraform Scope",
      "severity": "high",
      "category": "Infrastructure \u2014 Missing Detective Controls",
      "file": "deploy/regional/main.tf",
      "line": 1,
      "end_line": null,
      "issue": "The Terraform deployment contains no `aws_cloudtrail` resource, no `aws_guardduty_detector`, and no `aws_config_*` resources. While these may be deployed at the organization/account level separately, there is no validation or dependency ensuring they are enabled for this workload's region. There are no S3 data event trails for the audit bucket, no CloudTrail insights for API anomaly detection, and no explicit GuardDuty threat detection.",
      "impact": "For a FedRAMP system, missing CloudTrail coverage means API calls to ECS, EFS, Lambda, KMS, and IAM are not audited. FedRAMP controls AU-2 (Event Logging), AU-3 (Content of Audit Records), and AU-12 (Audit Record Generation) require that all security-relevant events are captured. Without CloudTrail data events on the S3 audit bucket, access to WORM audit evidence is not itself audited \u2014 creating an audit log that cannot itself be audited.",
      "recommendation": "Add CloudTrail with S3 data events and management events to the Terraform deployment:\n\n```hcl\nresource \"aws_cloudtrail\" \"main\" {\n  name                          = \"${var.project}-${var.stage}-trail\"\n  s3_bucket_name                = aws_s3_bucket.audit.id\n  include_global_service_events = true\n  is_multi_region_trail         = true\n  enable_log_file_validation    = true\n  kms_key_id                    = aws_kms_key.exec_session.arn\n\n  event_selector {\n    read_write_type           = \"All\"\n    include_management_events = true\n    data_resource {\n      type   = \"AWS::S3::Object\"\n      values = [\"${aws_s3_bucket.audit.arn}/\"]\n    }\n  }\n}\n```\n\nAlso add `aws_guardduty_detector` and consider `aws_securityhub_account` for FedRAMP compliance aggregation."
    },
    {
      "id": "H2",
      "title": "Lambda Function Not Deployed Inside VPC \u2014 JWKS Fetching Over Public Internet",
      "severity": "high",
      "category": "Infrastructure \u2014 Network Security",
      "file": "deploy/regional/lambda-create-investigation.tf",
      "line": 114,
      "end_line": 158,
      "issue": "The create-investigation Lambda has no `vpc_config` block, meaning it runs outside the VPC and fetches JWKS public keys from the Keycloak endpoint over the public internet. For a FedRAMP system where Keycloak may be hosted on OpenShift/ROSA, the Lambda must egress to a public endpoint to validate tokens. There are no VPC endpoints defined in the Terraform for Lambda, ECS, EFS, or Secrets Manager, suggesting all AWS API calls from the Lambda also go over the public internet.",
      "impact": "Token validation network path is over the public internet without VPC-level traffic inspection or control. If Keycloak is or becomes internet-facing, JWKS keys are fetched without intermediate network security controls. AWS API calls from the Lambda (ECS, EFS, STS) egress over the public internet rather than through VPC endpoints, potentially bypassing VPC-level SCPs or network policies. This violates FedRAMP SC-7 (Boundary Protection) requirements for controlled network boundaries.",
      "recommendation": "Deploy the Lambda inside the VPC with a VPC config:\n\n```hcl\nvpc_config {\n  subnet_ids         = var.subnet_ids\n  security_group_ids = [aws_security_group.lambda.id]\n}\n```\n\nAdd VPC Interface Endpoints for ECS, EFS, STS, SecretsManager, and Lambda to keep traffic private. Add a security group for the Lambda that allows only necessary egress (Keycloak HTTPS, AWS service endpoints)."
    },
    {
      "id": "H3",
      "title": "ABAC Tag Bypass: ExecuteCommandOnOwnedTasks Uses Resource Wildcard",
      "severity": "high",
      "category": "Infrastructure \u2014 Authorization Control Bypass",
      "file": "deploy/regional/oidc.tf",
      "line": 186,
      "end_line": 199,
      "issue": "The `ExecuteCommandOnOwnedTasks` statement grants `ecs:ExecuteCommand` on `Resource = \"*\"` with a StringEquals condition on `ecs:ResourceTag/<abac_tag_key>`. The `abac_tag_key` is configurable (default: `username`, or `uuid` for Red Hat EmployeeIDP). The StringEquals condition uses `$${aws:PrincipalTag/<key>}` which resolves at policy evaluation time from the STS session tag set during `AssumeRoleWithWebIdentity`.\n\nHowever, the `DescribeAndListECS` statement also grants `ecs:DescribeTasks`, `ecs:ListTasks`, and `ecs:DescribeTaskDefinition` on `Resource = \"*\"` with no conditions. This means any authenticated SRE can enumerate ALL tasks and task definitions in the cluster, including those belonging to other users.",
      "impact": "While an SRE cannot exec into another user's task (the ABAC condition prevents this), they can: (1) list all running tasks in the cluster including other users' investigation IDs, cluster IDs, and OIDC subjects from task tags; (2) describe all task definitions including investigation metadata baked into environment variables; (3) derive another user's active investigation state from tag data. This is an information disclosure vulnerability \u2014 the investigation isolation model leaks who is investigating what cluster, when, and with what timeout.",
      "recommendation": "Scope `DescribeAndListECS` to the authenticated user's own tasks using ABAC, or at minimum filter by cluster ARN:\n\n```hcl\n{\n  Sid    = \"DescribeAndListECS\"\n  Effect = \"Allow\"\n  Action = [\"ecs:DescribeTasks\", \"ecs:ListTasks\", \"ecs:DescribeTaskDefinition\"]\n  Resource = aws_ecs_cluster.main.arn\n}\n```\n\nFor task-level describe, add a separate statement with the ABAC condition matching task tag to principal tag."
    },
    {
      "id": "H4",
      "title": "Root Sudo ALL Enables Entrypoint SIGKILL to Bypass S3 Audit Sync",
      "severity": "high",
      "category": "Application \u2014 ZOA Audit Integrity Bypass",
      "file": "Containerfile",
      "line": 66,
      "end_line": null,
      "issue": "The SRE user has `NOPASSWD: ALL` sudo (Containerfile line 66). The entrypoint runs as root and is the container's PID 1. S3 audit sync runs only in `cleanup()` (SIGTERM/SIGINT/SIGHUP trap) and at normal exit. SIGKILL cannot be trapped. An SRE who connects via ECS Exec can execute `sudo kill -9 1` to send SIGKILL to the entrypoint process (PID 1), killing it instantly without triggering any signal handler. The container terminates and the S3 sync of /home/sre never runs.",
      "impact": "A malicious SRE can suppress the S3 audit evidence upload by killing the container entrypoint with SIGKILL before ECS stops the task. All investigation artifacts stored in /home/sre (commands run, downloaded data, must-gather output, notes) are permanently lost \u2014 not uploaded to the WORM audit bucket. This directly compromises the ZOA audit integrity guarantee. Combines with M8 (NOPASSWD: ALL sudo) and the architectural limitation that SIGKILL cannot be caught by bash signal traps.",
      "recommendation": "Remove `NOPASSWD: ALL` sudo (see M8) \u2014 restrict to specific binaries such as `/usr/sbin/alternatives` only. With no root access, the SRE cannot kill PID 1. Additionally, consider an ECS task lifecycle hook or sidecar container that syncs /home/sre continuously (e.g., via `inotifywait` or a periodic `aws s3 sync`) rather than only on exit, so evidence is preserved even on abnormal termination."
    },
    {
      "id": "H4",
      "title": "Root Sudo ALL Enables Entrypoint SIGKILL to Bypass S3 Audit Sync",
      "severity": "high",
      "category": "Application \u2014 ZOA Audit Integrity Bypass",
      "file": "entrypoint.sh",
      "line": 46,
      "end_line": 143,
      "issue": "The signal trap at line 46 covers SIGTERM, SIGINT, and SIGHUP but cannot catch SIGKILL. The S3 sync at line 143 executes at normal exit but not if the entrypoint is killed with signal 9. Since the entrypoint runs as PID 1 and the SRE has unrestricted sudo (M8), the SRE can `sudo kill -9 1` to terminate the process without triggering any cleanup.",
      "impact": "See H4 (Containerfile) \u2014 the S3 audit sync is the last line of ZOA audit integrity defense inside the container and can be bypassed by SIGKILL.",
      "recommendation": "Restrict SRE sudo to specific commands (see M8) to prevent killing PID 1. Implement a sidecar container or continuous S3 sync so evidence preservation does not depend solely on the entrypoint trap."
    },
    {
      "id": "H5",
      "title": "Authenticated SREs Can Request task_timeout=0 to Disable Reaper Enforcement",
      "severity": "high",
      "category": "Application \u2014 ZOA Session Timeout Bypass",
      "file": "lambda/create-investigation/handler.py",
      "line": 133,
      "end_line": 147,
      "issue": "The Lambda handler accepts `task_timeout` from the client request body and validates only that it is an integer 0-86400 (line 141-147). Any authenticated SRE can set `task_timeout=0` when calling `start-task`. When `task_timeout=0`, no `deadline` tag is added to the ECS task (handler.py line 694: `if task_timeout > 0:`). The reaper Lambda skips tasks with no `deadline` tag (reap-tasks/handler.py line 99: `if not deadline_str: skipped += 1; continue`). Additionally, the reaper's IAM `ecs:StopTask` permission has a condition `ForAnyValue:StringLike: {ecs:ResourceTag/deadline: \"*\"}` (lambda-reap-tasks.tf line 69-72), meaning even a manual reaper invocation cannot stop tasks without the tag.",
      "impact": "An authenticated SRE can bypass the tamper-proof timeout mechanism by requesting `task_timeout=0` at investigation creation time. The resulting task runs indefinitely \u2014 no reaper enforcement, no deadline tag, and the reaper IAM policy actively prevents the reaper from stopping it. This violates the ZOA requirement for bounded session duration. The task can persist until manually stopped by an operator with ECS console/CLI access. The CLAUDE.md states TASK_TIMEOUT is 'not modifiable from container' \u2014 this is correct, but it IS freely selectable at task creation by any authenticated SRE.",
      "recommendation": "Enforce a minimum task_timeout in the Lambda handler: reject `task_timeout=0` or set a minimum floor (e.g., 300 seconds). Alternatively, make `task_timeout` an operator-only setting controlled by the Terraform variable alone and remove it from the client request body entirely. The Terraform variable `task_timeout_default` exists for this purpose:\n\n```python\n# Remove task_timeout from body parsing; use Lambda env var only:\ntask_timeout = int(os.environ.get('TASK_TIMEOUT_DEFAULT', '3600'))\n```"
    },
    {
      "id": "H5",
      "title": "Authenticated SREs Can Request task_timeout=0 to Disable Reaper Enforcement",
      "severity": "high",
      "category": "Application \u2014 ZOA Session Timeout Bypass",
      "file": "deploy/regional/lambda-reap-tasks.tf",
      "line": 63,
      "end_line": 73,
      "issue": "The reaper IAM policy's `ecs:StopTask` permission requires `ForAnyValue:StringLike: {ecs:ResourceTag/deadline: \"*\"}`. This prevents the reaper from stopping any task without a `deadline` tag \u2014 including tasks where the SRE intentionally set `task_timeout=0`. The combination means tasks with no deadline tag are permanently unmanageable by the reaper.",
      "impact": "See H5 (handler.py). The IAM condition locks in the bypass: even a manual reaper Lambda invocation cannot reap a deadline-less task. An operator must use the ECS console or CLI directly.",
      "recommendation": "Remove the `ForAnyValue:StringLike` condition from the `ecs:StopTask` IAM statement. Deadline enforcement should be a handler-level decision, not an IAM gate. The current condition was intended as a safety guard but inadvertently creates an unmanageable task class."
    },
    {
      "id": "M15",
      "title": "No OIDC Token Replay Protection: jti Claim Not Tracked",
      "severity": "medium",
      "category": "Application \u2014 Authentication \u2014 Missing Replay Protection",
      "file": "lambda/create-investigation/handler.py",
      "line": 305,
      "end_line": 342,
      "issue": "The `_validate_with_jwks()` function validates the JWT signature, `exp`, and `aud` claims, but does not check the `jti` (JWT ID) claim and maintains no token replay store. PyJWT's `decode()` options at lines 316-325 do not include `jti` validation. A valid OIDC token intercepted from network traffic, the CLI token cache (`~/.cache/rosa-boundary/token-cache`), Lambda CloudWatch logs (before redaction was added), or a compromised workstation can be replayed to the Lambda within the token's `exp` window to create additional unauthorized investigations.",
      "impact": "A stolen OIDC token can be replayed for its full validity period (typically 5 minutes for Keycloak ID tokens by default, but configurable up to hours) to: (1) create investigations under the victim SRE's identity; (2) assume the lambda-invoker role via STS `AssumeRoleWithWebIdentity`; (3) assume the shared SRE role and exec into the victim's existing task. The absence of jti tracking means any token theft within its validity window grants full impersonation. For a FedRAMP system where all actions must be attributed to a specific individual, this is a significant audit and access control gap.",
      "recommendation": "Implement jti-based replay protection using DynamoDB or ElastiCache as a short-lived token store:\n\n```python\nimport boto3, time\ndynamodb = boto3.resource('dynamodb')\ntable = dynamodb.Table(os.environ['TOKEN_REPLAY_TABLE'])\n\ndef check_and_record_jti(claims):\n    jti = claims.get('jti')\n    if not jti:\n        return  # Tokens without jti are not replayable\n    exp = claims.get('exp', int(time.time()) + 300)\n    try:\n        table.put_item(\n            Item={'jti': jti, 'exp': exp},\n            ConditionExpression='attribute_not_exists(jti)'\n        )\n    except table.meta.client.exceptions.ConditionalCheckFailedException:\n        raise ValueError('Token already used (replay detected)')\n```\nAlternatively, configure Keycloak to issue short-lived tokens (60-second `exp`) and enforce STS role session duration constraints."
    },
    {
      "id": "M16",
      "title": "KMS Key Deletion Window Is 7 Days \u2014 Minimum Allowed, Insufficient for FedRAMP",
      "severity": "medium",
      "category": "Infrastructure \u2014 Key Management",
      "file": "deploy/regional/kms.tf",
      "line": 4,
      "end_line": null,
      "issue": "The `exec_session` KMS key has `deletion_window_in_days = 7`, which is the minimum AWS allows. This key protects ECS Exec session logs (SSM channel encryption) and CloudWatch log groups (when M3 is remediated). A scheduled key deletion, whether accidental or malicious, takes effect in 7 days.",
      "impact": "If the KMS key is accidentally scheduled for deletion (or an operator with `kms:ScheduleKeyDeletion` does so), all ECS Exec session logs and CloudWatch log data encrypted with this key become permanently unreadable after 7 days. For FedRAMP systems, audit evidence must be retained for defined periods (AU-11: 90 days online, 1 year offline). Losing the key renders the WORM-protected audit evidence inaccessible despite the S3 Object Lock. This also affects the investigation log integrity required by the ZOA model. 7 days is insufficient detection/response time for a FedRAMP incident response process.",
      "recommendation": "Increase the deletion window to 30 days (the AWS maximum for pending deletion) to give the security team time to detect and cancel accidental key deletion:\n\n```hcl\nresource \"aws_kms_key\" \"exec_session\" {\n  deletion_window_in_days = 30\n  ...\n}\n```\n\nAlso add a CloudWatch alarm on the `kms:ScheduleKeyDeletion` CloudTrail event for this key ARN."
    },
    {
      "id": "L11",
      "title": "task_timeout Accepted from Client Request Body Without Operator Maximum Enforcement",
      "severity": "low",
      "category": "Application \u2014 Authorization Policy Gap",
      "file": "lambda/create-investigation/handler.py",
      "line": 133,
      "end_line": null,
      "issue": "The Lambda handler accepts `task_timeout` from the client-supplied request body and validates only that it is an integer 0-86400. The operator configures a default via `TASK_TIMEOUT_DEFAULT` (Terraform `task_timeout_default`, default 3600), but any authenticated SRE can override this to any value up to 24 hours, including the maximum of 86400 seconds (24 hours).",
      "impact": "A legitimate SRE can request a 24-hour session even if the operator intended to enforce a 1-hour maximum. While this is not a critical bypass (the SRE is authenticated), it undermines operator policy and reduces the effectiveness of the tamper-proof timeout as a ZOA control. Combined with H5, if `task_timeout=0`, the timeout control is eliminated entirely.",
      "recommendation": "Add a configurable maximum task timeout that operators can set via a separate Lambda environment variable, and enforce it server-side:\n\n```python\nMAX_TASK_TIMEOUT = int(os.environ.get('MAX_TASK_TIMEOUT', '86400'))\n# ...\nif task_timeout > MAX_TASK_TIMEOUT:\n    return response(400, {'error': f'task_timeout exceeds maximum allowed value of {MAX_TASK_TIMEOUT} seconds'})\n```\n\nThis allows operators to set a deployment-specific ceiling independently of the default."
    }
  ]
}
